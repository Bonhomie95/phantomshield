{
  "name": "phantomshield-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "PhantomShield monorepo — mobile app, backend API, and web dashboard",
  "//workspaces-note": "mobile is intentionally NOT a workspace. Expo/Metro/babel-preset-expo require the app's deps (expo-router, reanimated, worklets, react-native-web) to be co-located in mobile/node_modules; npm hoisting splits them and silently disables those babel plugins. Mobile is a standalone install that links @phantomshield/shared via a file: dependency.",
  "workspaces": [
    "packages/*",
    "backend",
    "dashboard"
  ],
  "//overrides-note": "Pin one TypeScript across all workspaces. ts-node-dev peers on `typescript@*`, which otherwise lets npm auto-install a newer major (6.x) nested under backend, skewing it from the 5.9.x the rest of the repo (and the Node 20 runtime types) use.",
  "overrides": {
    "typescript": "5.9.3"
  },
  "scripts": {
    "build:shared": "npm --workspace @phantomshield/shared run build",
    "typecheck": "npm run build:shared && npm run typecheck --workspaces --if-present && npm run typecheck:mobile",
    "typecheck:mobile": "npm --prefix mobile run typecheck",
    "install:all": "npm install && npm --prefix mobile install",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present && npm run lint:mobile",
    "lint:mobile": "npm --prefix mobile run lint"
  }
}
